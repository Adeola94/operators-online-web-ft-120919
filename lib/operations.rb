def unsafe = 79
    if speed > 60
      puts ("unsafe")
    end
    
    def safe = 35
 if speed < 40
   puts ("safe")
    end
if speed 40 == 60

it 'returns false if the speed is between 40 and 60' do
      answer = unsafe?(50)
      expect(answer).to eq(false)